# eng/pipelines/typespec-break-check.yml
# JS SDK TypeSpec Breaking Change Check Pipeline (ARM Only)
# Purpose: Validate that the latest emitter (@azure-tools/typespec-ts) doesn't break existing ARM SDK packages
# Flow: Fetch latest emitter version → update emitter-package.json → regenerate lock file
#       → clone latest azure-rest-api-specs (local spec source) → build spec index from tspconfig.yaml
#       → classify SDK packages (spec-index preferred, tsp-location fallback)
#       → tsp-client init --update-if-exists -c <local-tspconfig.yaml> --local-spec-repo (concurrent)
#       → pnpm build (concurrent)

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - eng/emitter-package.json
      - eng/common/scripts/TypeSpec-*
      - eng/common/tsp-client/
      - eng/pipelines/typespec-break-check.yml
      - eng/pipelines/scripts/regenerate-runner.js
      - eng/pipelines/scripts/breaking-change-detector.js

pr:
  branches:
    include:
      - main
      - feature/*
      - hotfix/*
  paths:
    include:
      - eng/emitter-package.json
      - eng/common/scripts/TypeSpec-*
      - eng/common/tsp-client/
      - eng/pipelines/typespec-break-check.yml
      - eng/pipelines/scripts/regenerate-runner.js
      - eng/pipelines/scripts/breaking-change-detector.js

schedules:
  - cron: "0 2 * * 1"
    displayName: Weekly TypeSpec break check (Monday 2AM UTC)
    branches:
      include:
        - main

parameters:
  - name: EmitterVersion
    displayName: "Emitter version to test (use 'empty' or leave default for latest dev tag)"
    type: string
    default: 'empty'
  - name: MaxPackages
    displayName: "Max ARM packages to process (0 = all)"
    type: number
    default: 0
  - name: MaxWorkers
    displayName: "Regenerate concurrent workers"
    type: number
    default: 2
  - name: BuildWorkers
    displayName: "Build concurrent workers"
    type: number
    default: 1
  - name: RegenerationJobCount
    displayName: "Matrix jobs for regeneration"
    type: number
    default: 10
  - name: MinimumPerJob
    displayName: "Minimum packages per matrix job"
    type: number
    default: 10
  - name: SkipBuild
    displayName: "Skip build verification"
    type: boolean
    default: false
  - name: SpecRepoBranch
    displayName: "azure-rest-api-specs branch to clone"
    type: string
    default: 'main'
  - name: CreatePullRequest
    displayName: "Create PR with generated changes"
    type: boolean
    default: false
  - name: ChangePushMode
    displayName: "Changes included in PR commit"
    type: string
    default: 'all'
    values:
      - all
      - api-md
  - name: PullRequestTargetBranch
    displayName: "PR target branch"
    type: string
    default: 'main'
  - name: PullRequestBranch
    displayName: "PR source branch (empty = auto)"
    type: string
    default: ''
  - name: PullRequestRepoOwner
    displayName: "PR target repo OWNER (empty = same as source repo, e.g. 'wxl534' for fork)"
    type: string
    default: ''
  - name: PullRequestRepoName
    displayName: "PR target repo NAME (empty = same as source repo name)"
    type: string
    default: ''
  - name: ForkTokenVariableName
    displayName: "ADO secret variable name holding the GitHub PAT (only needed when pushing to a fork)"
    type: string
    default: ''

pool:
  vmImage: 'ubuntu-latest'

variables:
  sdkRoot: $(Build.SourcesDirectory)
  specRepoRoot: $(Agent.BuildDirectory)/azure-rest-api-specs
  nodeVersion: '22.x'

stages:
  - stage: Regenerate
    displayName: 'Regenerate & Build SDK packages'
    jobs:
      - job: Setup
        displayName: 'Resolve emitter version & prepare'
        steps:
          - task: NodeTool@0
            displayName: 'Install Node.js'
            inputs:
              versionSpec: $(nodeVersion)

          - script: npm install -g @azure-tools/typespec-client-generator-cli
            displayName: 'Install tsp-client'

          # Step 1: Resolve emitter version
          - script: |
              REPO_EMITTER_VERSION=$(node -e "const p=require('./eng/emitter-package.json'); console.log(p.dependencies['@azure-tools/typespec-ts'] || '')")
              echo "Emitter currently committed in repo: $REPO_EMITTER_VERSION"

              # Treat empty / common sentinel values ('empty', 'latest', 'auto', 'dev')
              # as "use the latest dev tag from npm", because the ADO manual-run UI
              # forces string params to be non-empty.
              RAW_INPUT='${{ parameters.EmitterVersion }}'
              NORMALIZED=$(echo "$RAW_INPUT" | tr '[:upper:]' '[:lower:]' | xargs)
              if [ -z "$NORMALIZED" ] || [ "$NORMALIZED" = "empty" ] || [ "$NORMALIZED" = "latest" ] || [ "$NORMALIZED" = "auto" ] || [ "$NORMALIZED" = "dev" ]; then
                EMITTER_VERSION=$(npm view @azure-tools/typespec-ts dist-tags.dev)
                echo "Resolved from npm dev tag: $EMITTER_VERSION"
              else
                EMITTER_VERSION="$RAW_INPUT"
                echo "Using specified version: $EMITTER_VERSION"
              fi

              # Find the *npm-published* version that comes right before the current
              # one, so that the breaking-change diff reflects this release's delta
              # (not just whatever stale version happens to be in the repo).
              PREVIOUS_EMITTER_VERSION=$(node -e "
                const { execSync } = require('child_process');
                const target = process.argv[1];
                const versions = JSON.parse(execSync('npm view @azure-tools/typespec-ts versions --json', { encoding: 'utf8' }));
                const idx = versions.indexOf(target);
                if (idx > 0) {
                  console.log(versions[idx - 1]);
                } else if (versions.length > 0) {
                  // target is not on npm (e.g. local build) — fall back to latest published
                  console.log(versions[versions.length - 1]);
                }
              " "$EMITTER_VERSION")

              if [ -z "$PREVIOUS_EMITTER_VERSION" ]; then
                echo "Warning: could not resolve previous npm version, falling back to repo version"
                PREVIOUS_EMITTER_VERSION="$REPO_EMITTER_VERSION"
              fi

              echo "Previous emitter version (one release before current): $PREVIOUS_EMITTER_VERSION"
              echo "Will compare API surface: $PREVIOUS_EMITTER_VERSION -> $EMITTER_VERSION"

              echo "##vso[task.setvariable variable=emitterVersion;isOutput=true]$EMITTER_VERSION"
              echo "##vso[task.setvariable variable=previousEmitterVersion;isOutput=true]$PREVIOUS_EMITTER_VERSION"
              echo "##vso[task.setvariable variable=repoEmitterVersion;isOutput=true]$REPO_EMITTER_VERSION"
            displayName: 'Step 1: Resolve emitter version'
            name: resolveVersion

          # Step 2: Update emitter-package.json + regenerate lock file
          - script: |
              cd $(sdkRoot)
              EMITTER_VERSION=$(resolveVersion.emitterVersion)

              # Update emitter-package.json
              node -e "
                const fs = require('fs');
                const pkg = JSON.parse(fs.readFileSync('eng/emitter-package.json', 'utf8'));
                const oldVer = pkg.dependencies['@azure-tools/typespec-ts'];
                pkg.dependencies['@azure-tools/typespec-ts'] = '$EMITTER_VERSION';
                fs.writeFileSync('eng/emitter-package.json', JSON.stringify(pkg, null, 2) + '\n');
                console.log('Updated: ' + oldVer + ' -> $EMITTER_VERSION');
              "

              # Regenerate lock file to prevent npm ci mismatch
              cd eng
              cp emitter-package.json package.json
              npm install --package-lock-only --ignore-scripts 2>/dev/null
              if [ -f package-lock.json ]; then
                cp package-lock.json emitter-package-lock.json
                rm -f package.json package-lock.json
                rm -rf node_modules
                echo "Lock file regenerated ✅"
              else
                echo "Warning: Failed to regenerate lock file"
              fi
            displayName: 'Step 2: Update emitter + regenerate lock file'

      - job: GenerateMatrix
        displayName: 'Generate package matrix'
        dependsOn: Setup
        steps:
          - task: PowerShell@2
            displayName: 'Split ARM packages into matrix batches'
            name: generateMatrix
            inputs:
              pwsh: true
              filePath: ./eng/common/scripts/New-RegenerateMatrix.ps1
              arguments: >
                -OutputDirectory "$(Build.ArtifactStagingDirectory)/matrix"
                -OutputVariableName matrix
                -JobCount ${{ parameters.RegenerationJobCount }}
                -MinimumPerJob ${{ parameters.MinimumPerJob }}
                -MaxPackages ${{ parameters.MaxPackages }}
                -OnlyTypeSpec false
                -DirectoryFilterPattern 'arm-*'
              workingDirectory: $(Build.SourcesDirectory)

          - task: PublishPipelineArtifact@1
            displayName: 'Publish matrix artifacts'
            inputs:
              targetPath: $(Build.ArtifactStagingDirectory)/matrix
              artifact: matrix_artifacts

      - job: RegenerateAndBuild
        displayName: 'Regenerate & build packages'
        dependsOn:
          - Setup
          - GenerateMatrix
        timeoutInMinutes: 180
        strategy:
          matrix: $[ dependencies.GenerateMatrix.outputs['generateMatrix.matrix'] ]
        variables:
          emitterVersion: $[ dependencies.Setup.outputs['resolveVersion.emitterVersion'] ]
          previousEmitterVersion: $[ dependencies.Setup.outputs['resolveVersion.previousEmitterVersion'] ]
        steps:
          - task: NodeTool@0
            displayName: 'Install Node.js'
            inputs:
              versionSpec: $(nodeVersion)

          - script: |
              npm install -g @azure-tools/typespec-client-generator-cli
              npm install -g pnpm
              npm config set legacy-peer-deps true
            displayName: 'Install tsp-client and pnpm'

          - download: current
            displayName: 'Download matrix artifacts'
            artifact: matrix_artifacts

          # Clone azure-rest-api-specs (latest main) as a sibling directory.
          - script: |
              if [ -d "$(specRepoRoot)" ]; then
                echo "Spec repo already exists at $(specRepoRoot), pulling latest..."
                cd $(specRepoRoot)
                git fetch origin ${{ parameters.SpecRepoBranch }}
                git checkout ${{ parameters.SpecRepoBranch }}
                git pull --ff-only
              else
                echo "Cloning azure-rest-api-specs (branch ${{ parameters.SpecRepoBranch }})..."
                git clone --depth 1 --branch ${{ parameters.SpecRepoBranch }} \
                  https://github.com/Azure/azure-rest-api-specs.git $(specRepoRoot)
              fi
              echo "Spec repo ready at $(specRepoRoot)"
              cd $(specRepoRoot) && git log -1 --oneline
            displayName: 'Clone azure-rest-api-specs (local spec source)'

          # Update emitter-package.json + lock file
          - script: |
              cd $(sdkRoot)
              node -e "
                const fs = require('fs');
                const pkg = JSON.parse(fs.readFileSync('eng/emitter-package.json', 'utf8'));
                pkg.dependencies['@azure-tools/typespec-ts'] = '$(emitterVersion)';
                fs.writeFileSync('eng/emitter-package.json', JSON.stringify(pkg, null, 2) + '\n');
              "
              cd eng
              cp emitter-package.json package.json
              npm install --package-lock-only --ignore-scripts 2>/dev/null
              if [ -f package-lock.json ]; then
                cp package-lock.json emitter-package-lock.json
                rm -f package.json package-lock.json
                rm -rf node_modules
              fi
            displayName: 'Update emitter + lock file'

          # Step 3-5: Build spec index → classify packages → regenerate → build
          - script: |
              cd $(sdkRoot)
              mkdir -p "$(Build.ArtifactStagingDirectory)/results/$(JobKey)"
              node eng/pipelines/scripts/regenerate-runner.js \
                --specRepoRoot "$(specRepoRoot)" \
                --maxWorkers "${{ parameters.MaxWorkers }}" \
                --buildWorkers "${{ parameters.BuildWorkers }}" \
                --skipBuild "${{ parameters.SkipBuild }}" \
                --emitterVersion "$(emitterVersion)" \
                --directoryList "$(Pipeline.Workspace)/matrix_artifacts/$(DirectoryList)" \
                --resultOutputDir "$(Build.ArtifactStagingDirectory)/results/$(JobKey)"
            displayName: 'Regenerate & build ARM package batch'

          # Step 6: Breaking Change Detection
          - script: |
              cd $(sdkRoot)
              node eng/pipelines/scripts/breaking-change-detector.js \
                --baseline "HEAD" \
                --previousEmitterVersion "$(previousEmitterVersion)" \
                --currentEmitterVersion "$(emitterVersion)" \
                --reportFile "$(Build.ArtifactStagingDirectory)/results/$(JobKey)/breaking-summary.json" \
                --no-fail \
                --verbose
            displayName: 'Step 6: Detect breaking changes'
            condition: succeededOrFailed()

          - script: |
              cd $(sdkRoot)
              PATCH_FILE="$(Build.ArtifactStagingDirectory)/results/$(JobKey)/changes.patch"
              MODE="${{ parameters.ChangePushMode }}"
              if [ "$MODE" = "api-md" ]; then
                git diff --binary -- ':(glob)sdk/**/review/*.api.md' > "$PATCH_FILE"
              else
                git diff --binary -- sdk/ > "$PATCH_FILE"
              fi
              echo "Patch mode: $MODE"
              echo "Patch file: $PATCH_FILE"
              wc -l "$PATCH_FILE" || true
            displayName: 'Create patch for PR job'
            condition: and(always(), ${{ parameters.CreatePullRequest }})

          - script: |
              cd $(sdkRoot)
              git diff --stat
            displayName: 'Show generated changes'
            condition: always()

          - task: PublishPipelineArtifact@1
            displayName: 'Publish batch result'
            condition: always()
            inputs:
              targetPath: $(Build.ArtifactStagingDirectory)/results/$(JobKey)
              artifact: regen_result_$(JobKey)

      - job: Summary
        displayName: 'Summarize matrix results'
        dependsOn: RegenerateAndBuild
        condition: succeededOrFailed()
        steps:
          - task: NodeTool@0
            displayName: 'Install Node.js'
            inputs:
              versionSpec: $(nodeVersion)

          - download: current
            displayName: 'Download result artifacts'

          - script: |
              node - <<'NODE'
              const fs = require("fs");
              const path = require("path");

              const root = process.env.PIPELINE_WORKSPACE;
              const resultFiles = [];
              const breakingSummaryFiles = [];

              function walk(dir) {
                if (!fs.existsSync(dir)) return;
                for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
                  const full = path.join(dir, entry.name);
                  if (entry.isDirectory()) walk(full);
                  else if (entry.name === "result.json") resultFiles.push(full);
                  else if (entry.name === "breaking-summary.json") breakingSummaryFiles.push(full);
                }
              }

              walk(root);

              const totals = {
                packages: 0,
                regenTotal: 0,
                regenSuccess: 0,
                regenFailed: 0,
                buildTotal: 0,
                buildSuccess: 0,
                buildFailed: 0,
                regenFailures: [],
                buildFailures: [],
                breakingTotal: 0,
                potentiallyBreakingTotal: 0,
                additiveTotal: 0,
                changedPackages: [],
                previousEmitterVersion: null,
                currentEmitterVersion: null,
              };

              for (const file of resultFiles) {
                const result = JSON.parse(fs.readFileSync(file, "utf8"));
                totals.packages += result.packages?.length ?? 0;
                totals.regenTotal += result.regeneration?.total ?? 0;
                totals.regenSuccess += result.regeneration?.success ?? 0;
                totals.regenFailed += result.regeneration?.failed ?? 0;
                totals.regenFailures.push(...(result.regeneration?.failures ?? []));

                if (result.build) {
                  totals.buildTotal += result.build.total ?? 0;
                  totals.buildSuccess += result.build.success ?? 0;
                  totals.buildFailed += result.build.failed ?? 0;
                  totals.buildFailures.push(...(result.build.failures ?? []));
                }
              }

              for (const file of breakingSummaryFiles) {
                const summary = JSON.parse(fs.readFileSync(file, "utf8"));
                totals.breakingTotal += summary.totalBreaking ?? 0;
                totals.potentiallyBreakingTotal += summary.totalPotentiallyBreaking ?? 0;
                totals.additiveTotal += summary.totalAdditive ?? 0;
                totals.changedPackages.push(...(summary.changedPackages ?? []));
                if (!totals.previousEmitterVersion) totals.previousEmitterVersion = summary.previousEmitterVersion;
                if (!totals.currentEmitterVersion) totals.currentEmitterVersion = summary.currentEmitterVersion;
              }

              console.log("========== MATRIX SUMMARY ==========");
              console.log(`Result files: ${resultFiles.length}`);
              console.log(`Packages: ${totals.packages}`);
              console.log("--- Regeneration ---");
              console.log(`Total: ${totals.regenTotal} | Success: ${totals.regenSuccess} | Failed: ${totals.regenFailed}`);
              console.log(`Success Rate: ${totals.regenTotal === 0 ? "0.0" : ((totals.regenSuccess * 100) / totals.regenTotal).toFixed(1)}%`);
              console.log("--- Build ---");
              console.log(`Total: ${totals.buildTotal} | Build OK: ${totals.buildSuccess} | Build Failed: ${totals.buildFailed}`);
              console.log(`Build Pass Rate: ${totals.buildTotal === 0 ? "0.0" : ((totals.buildSuccess * 100) / totals.buildTotal).toFixed(1)}%`);
              console.log("--- API Change Signal ---");
              console.log(`Emitter under test : ${totals.currentEmitterVersion || "(unknown)"}`);
              console.log(`Previous on npm    : ${totals.previousEmitterVersion || "(unknown)"} (informational; not used as baseline)`);
              console.log(`Baseline           : api.md committed in git HEAD`);
              console.log(`Breaking: ${totals.breakingTotal} | Potentially Breaking: ${totals.potentiallyBreakingTotal} | Additive: ${totals.additiveTotal}`);

              if (totals.changedPackages.length > 0) {
                const merged = new Map();
                for (const p of totals.changedPackages) {
                  const existing = merged.get(p.pkg) || {
                    pkg: p.pkg,
                    totalChanges: 0,
                    breaking: 0,
                    potentiallyBreaking: 0,
                    additive: 0,
                  };
                  existing.totalChanges += p.totalChanges ?? 0;
                  existing.breaking += p.breaking ?? 0;
                  existing.potentiallyBreaking += p.potentiallyBreaking ?? 0;
                  existing.additive += p.additive ?? 0;
                  merged.set(p.pkg, existing);
                }
                const unique = [...merged.values()].sort((a, b) => (b.totalChanges ?? 0) - (a.totalChanges ?? 0));
                console.log("Top changed packages:");
                for (const p of unique.slice(0, 20)) {
                  console.log(`  - ${p.pkg}: total=${p.totalChanges}, breaking=${p.breaking}, potentiallyBreaking=${p.potentiallyBreaking}, additive=${p.additive}`);
                }
              }

              if (totals.regenFailures.length > 0) {
                console.log("");
                console.log("Regeneration failures:");
                for (const failure of totals.regenFailures) {
                  console.log(`  - ${failure.pkg}: ${failure.error}`);
                }
              }

              if (totals.buildFailures.length > 0) {
                console.log("");
                console.log("Build failures:");
                for (const failure of totals.buildFailures) {
                  console.log(`  - ${failure.pkg} (failed at ${failure.phase}): ${failure.error}`);
                }
              }
              console.log("====================================");

              if (resultFiles.length === 0 || totals.regenFailed > 0 || totals.buildFailed > 0) {
                process.exit(1);
              }
              NODE
            displayName: 'Aggregate regeneration results'

      - ${{ if parameters.CreatePullRequest }}:
        - job: CreatePR
          displayName: 'Create PR with generated changes'
          dependsOn:
            - Setup
            - Summary
          condition: succeeded()
          variables:
            emitterVersion: $[ dependencies.Setup.outputs['resolveVersion.emitterVersion'] ]
            previousEmitterVersion: $[ dependencies.Setup.outputs['resolveVersion.previousEmitterVersion'] ]
          steps:
            - checkout: self

            # Only request the org-level GitHub App token when we're NOT pushing to a fork
            # (otherwise the fork user's PAT is used and this template would needlessly
            # require Azure Key Vault access).
            - ${{ if eq(parameters.PullRequestRepoOwner, '') }}:
              - template: /eng/common/pipelines/templates/steps/login-to-github.yml
                parameters:
                  TokenOwners:
                    - Azure

            - download: current
              displayName: 'Download result artifacts'

            - script: |
                set -euo pipefail
                cd $(Build.SourcesDirectory)

                # Determine target repo (defaults to the source repo when no fork override)
                overrideOwner='${{ parameters.PullRequestRepoOwner }}'
                overrideName='${{ parameters.PullRequestRepoName }}'

                sourceRepoFull="$(Build.Repository.Name)"
                if [[ "$sourceRepoFull" == */* ]]; then
                  srcOwner="${sourceRepoFull%%/*}"
                  srcName="${sourceRepoFull##*/}"
                else
                  srcOwner="Azure"
                  srcName="$sourceRepoFull"
                fi

                repoOwner="${overrideOwner:-$srcOwner}"
                repoName="${overrideName:-$srcName}"
                isFork="false"
                if [ "$repoOwner" != "$srcOwner" ] || [ "$repoName" != "$srcName" ]; then
                  isFork="true"
                fi

                # Resolve the token to use for pushing.
                # FORK_TOKEN / GH_TOKEN_VAL are injected via the env: block below so
                # that ADO can correctly substitute secret variables at runtime.
                if [ "$isFork" = "true" ]; then
                  pushToken="${FORK_TOKEN:-}"
                  if [ -z "$pushToken" ]; then
                    echo "ERROR: Pushing to fork ${repoOwner}/${repoName} but no fork PAT was provided."
                    echo "Set parameter ForkTokenVariableName to the name of an ADO secret variable containing a GitHub PAT with repo scope,"
                    echo "and make sure that secret variable actually exists in this pipeline."
                    exit 1
                  fi
                  echo "Pushing to fork: ${repoOwner}/${repoName} (using user-provided PAT)"
                else
                  pushToken="${GH_TOKEN_VAL:-}"
                  echo "Pushing to source repo: ${repoOwner}/${repoName} (using GH App token)"
                fi

                branch="${{ parameters.PullRequestBranch }}"
                if [ -z "$branch" ]; then
                  branch="typespec-break-check-$(Build.BuildId)"
                fi
                echo "Using PR branch: $branch"

                git checkout -B "$branch"

                mapfile -t patches < <(find "$(Pipeline.Workspace)" -type f -name changes.patch | sort)
                echo "Found ${#patches[@]} patch file(s)"
                for p in "${patches[@]}"; do
                  if [ -s "$p" ]; then
                    echo "Applying patch: $p"
                    if ! git apply --3way "$p"; then
                      echo "Failed to apply patch: $p"
                      git status --porcelain || true
                      exit 1
                    fi
                  fi
                done

                mode="${{ parameters.ChangePushMode }}"
                if [ "$mode" = "api-md" ]; then
                  git add -A -- ':(glob)sdk/**/review/*.api.md'
                else
                  git add -A sdk/
                fi

                if git diff --cached --quiet; then
                  echo "No changes selected for mode=$mode. Skipping PR creation."
                  echo "##vso[task.setvariable variable=HasPrChanges]false"
                  exit 0
                fi

                git -c user.name="azure-sdk" -c user.email="azuresdk@microsoft.com" commit -m "TypeSpec regeneration changes (${mode}) for emitter $(previousEmitterVersion) -> $(emitterVersion)"

                git remote remove pr-origin 2>/dev/null || true
                git remote add pr-origin "https://x-access-token:${pushToken}@github.com/${repoOwner}/${repoName}.git"
                git push pr-origin "$branch" --force

                echo "##vso[task.setvariable variable=HasPrChanges]true"
                echo "##vso[task.setvariable variable=PRBranchName]$branch"
                echo "##vso[task.setvariable variable=RepoOwner]$repoOwner"
                echo "##vso[task.setvariable variable=RepoNameOnly]$repoName"
                echo "##vso[task.setvariable variable=IsFork]$isFork"
                echo "##vso[task.setvariable variable=PushToken;issecret=true]$pushToken"
              displayName: 'Apply patches, commit and push branch'
              env:
                # Map secret variables into the script via env. This is the only
                # reliable way for ADO to inject secret variables — `$(name)` direct
                # substitution and `$[ variables[...] ]` indirect lookup do NOT work
                # reliably for secret variables.
                ${{ if ne(parameters.ForkTokenVariableName, '') }}:
                  FORK_TOKEN: $(${{ parameters.ForkTokenVariableName }})
                ${{ if eq(parameters.PullRequestRepoOwner, '') }}:
                  GH_TOKEN_VAL: $(GH_TOKEN)

            - task: PowerShell@2
              displayName: 'Create pull request'
              condition: and(succeeded(), eq(variables['HasPrChanges'], 'true'))
              inputs:
                pwsh: true
                filePath: ./eng/common/scripts/Submit-PullRequest.ps1
                arguments: >
                  -RepoOwner '$(RepoOwner)'
                  -RepoName '$(RepoNameOnly)'
                  -BaseBranch '${{ parameters.PullRequestTargetBranch }}'
                  -PROwner '$(RepoOwner)'
                  -PRBranch '$(PRBranchName)'
                  -AuthToken '$(PushToken)'
                  -PRTitle 'TypeSpec emitter delta: $(previousEmitterVersion) -> $(emitterVersion) [${{ parameters.ChangePushMode }}]'
                  -PRBody 'Generated by $(Build.DefinitionName) build $(Build.BuildNumber). Compares emitter $(previousEmitterVersion) -> $(emitterVersion). Push mode: ${{ parameters.ChangePushMode }}.'
